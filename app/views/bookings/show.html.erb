<h1>Booking Details</h1>
<p>Date: <%= @booking.date %></p>
<p>Customer: <%= @booking.customer_name %></p>
<%= link_to 'Delete Booking', booking_path(@booking), method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back to Bookings', bookings_path %>
